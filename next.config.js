/** @type {import('next').NextConfig} */
const nextConfig = {
  // In Next.js 15+, allowedDevOrigins is a top-level key
  allowedDevOrigins: ['192.168.29.15', 'localhost', '127.0.0.1'],
};

module.exports = nextConfig;
